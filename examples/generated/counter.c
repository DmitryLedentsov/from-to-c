/* Generated by ftc. Do not edit by hand. */
#include <stdbool.h>
#include <stdio.h>

#define print_int(value) printf("%d\n", (value))
#define print_float(value) printf("%g\n", (value))

typedef struct Counter {
    int value;
} Counter;

static Counter Counter_init(int initial);
static void Counter_increment(Counter* self);
static int Counter_get(Counter* self);
int main(void);

static Counter Counter_init(int initial)
{
    Counter self_value = {0};
    Counter* self = &self_value;
    self->value = initial;
    return self_value;
}

static void Counter_increment(Counter* self)
{
    self->value = (self->value + 1);
}

static int Counter_get(Counter* self)
{
    return self->value;
}

int main(void)
{
    Counter counter = Counter_init(0);
    while ((Counter_get(&counter) < 3)) {
        Counter_increment(&counter);
        print_int(Counter_get(&counter));
    }
    return 0;
}
