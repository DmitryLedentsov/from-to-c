/* Generated by ftc. Do not edit by hand. */
#include <stdbool.h>
#include <stdio.h>

#define print_int(value) printf("%d\n", (value))
#define print_float(value) printf("%g\n", (value))

typedef struct Point {
    double x;
    double y;
} Point;

static Point Point_init(double x, double y);
static void Point_move(Point* self, double dx, double dy);
static double Point_sum(Point* self);
int main(void);

static Point Point_init(double x, double y)
{
    Point self_value = {0};
    Point* self = &self_value;
    self->x = x;
    self->y = y;
    return self_value;
}

static void Point_move(Point* self, double dx, double dy)
{
    self->x = (self->x + dx);
    self->y = (self->y + dy);
}

static double Point_sum(Point* self)
{
    return (self->x + self->y);
}

int main(void)
{
    Point point = Point_init(1.5, 2.5);
    Point_move(&point, 1.0, 2.0);
    print_float(Point_sum(&point));
    return 0;
}
